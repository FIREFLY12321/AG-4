name: CI/CD Deployment Pipeline (Outstanding Level)

permissions:
  contents: write

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # ===============================
  # 🏗️ Job 1：建置 + 測試 + 上傳 artifact
  # ===============================
  build:
    name: Build and Test (Matrix)
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.NEW_TAG }}
    strategy:
      matrix:
        node: [18, 20]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Install Bun runtime
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Generate Semantic Version
        id: version
        run: |
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          NEW_TAG=$(echo $LAST_TAG | awk -F. -v OFS=. '{$NF += 1 ; print}')
          echo "NEW_TAG=$NEW_TAG" >> $GITHUB_OUTPUT
          echo "📦 New Version: $NEW_TAG"

      - name: Generate Changelog
        id: changelog
        run: |
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          echo "🧾 Generating changelog since $LAST_TAG..."
          git log $LAST_TAG..HEAD --oneline > changelog.txt
          echo "CHANGELOG<<EOF" >> $GITHUB_ENV
          cat changelog.txt >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Install dependencies and build
        run: |
          echo "📦 Installing dependencies..."
          bun install
          echo "🏗️ Building project..."
          bun run build
          zip -r build.zip .

      - name: Run Lint and Tests (Tolerant Gate)
        run: |
          echo "🔍 Running lint..."
          if bun run | grep -q "lint"; then
            bun run lint
          else
            echo "⚠️ No lint script found, skipping lint."
          fi

          echo "🧪 Running tests..."
          if bun run | grep -q "test"; then
            bun run test
          else
            echo "⚠️ No test script found, skipping tests."
          fi

      - name: Upload build artifact (unique per Node)
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact-${{ matrix.node }}
          path: build.zip
          retention-days: 1

  # ===============================
  # 🚀 Job 2：部署至 dev 環境
  # ===============================
  deploy-dev:
    name: Deploy to Development
    needs: build
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
          
      - name: Install GitHub CLI
        run: sudo apt install gh -y
          
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: build-artifact-*
          merge-multiple: true
            
      - name: Verify artifact
        run: ls -lh
  
      - name: Create or Update Dev Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEV_TOKEN: ${{ secrets.DEV_TOKEN }}
        run: |
          TAG="dev-${{ needs.build.outputs.version }}"
          NOTE="🧾 Changelog:\n${CHANGELOG}\n\nDevelopment build $TAG"
          echo "🚀 Deploying $TAG"
          if gh release view "$TAG" > /dev/null 2>&1; then
            gh release upload "$TAG" build.zip --clobber
            gh release edit "$TAG" --notes "$NOTE"
          else
            gh release create "$TAG" build.zip --notes "$NOTE"
          fi
          echo "🔐 Using DEV token (len=${#DEV_TOKEN})"

      - name: Notify PR / Issue
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_URL=$(gh pr list --state open --json url --jq '.[0].url' || echo "")
          if [ -n "$PR_URL" ]; then
            gh pr comment "$PR_URL" --body "✅ Dev deployment done for $TAG"
          else
            echo "⚠️ No open PR found to comment on."
          fi

  # ===============================
  # 🧪 Job 3：部署至 staging 環境
  # ===============================
  deploy-staging:
    name: Deploy to Staging
    needs: [build, deploy-dev]
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
  
      - name: Install GitHub CLI
        run: sudo apt install gh -y
  
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: build-artifact-*
          merge-multiple: true
  
      - name: Create or Update Staging Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STG_TOKEN: ${{ secrets.STG_TOKEN }}
        run: |
          TAG="staging-${{ needs.build.outputs.version }}"
          NOTE="🧪 Staging build $TAG\n\n${CHANGELOG}"
          echo "🚀 Deploying staging release: $TAG"
          if gh release view "$TAG" > /dev/null 2>&1; then
            gh release upload "$TAG" build.zip --clobber
            gh release edit "$TAG" --notes "$NOTE"
          else
            gh release create "$TAG" build.zip --notes "$NOTE"
          fi
          echo "🔐 Using STG token (len=${#STG_TOKEN})"

      - name: Slack Notification
        if: always()
        env:
          WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data "{\"text\":\"🧩 Staging release *${{ needs.build.outputs.version }}* completed successfully.\"}" \
            $WEBHOOK_URL || echo "⚠️ Slack not configured"

      - name: Notify PR / Issue
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_URL=$(gh pr list --state open --json url --jq '.[0].url' || echo "")
          if [ -n "$PR_URL" ]; then
            gh pr comment "$PR_URL" --body "✅ Staging deployment done for $TAG"
          fi

  # ===============================
  # 🎯 Job 4：部署至 production 環境（需人工批准）
  # ===============================
  deploy-production:
    name: Deploy to Production
    needs: [build, deploy-staging]
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install GitHub CLI
        run: sudo apt install gh -y

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: build-artifact-*
          merge-multiple: true

      - name: Create Production Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PROD_TOKEN: ${{ secrets.PROD_TOKEN }}
        run: |
          TAG="prod-${{ needs.build.outputs.version }}"
          NOTE="🚀 Official production release\n\n${CHANGELOG}"
          echo "🚀 Creating production release: $TAG"
          if gh release view "$TAG" > /dev/null 2>&1; then
            gh release upload "$TAG" build.zip --clobber
            gh release edit "$TAG" --notes "$NOTE"
          else
            gh release create "$TAG" build.zip --notes "$NOTE"
          fi
          echo "🔐 Using PROD token (len=${#PROD_TOKEN})"

      - name: Rollback on Failure
        if: failure()
        run: |
          echo "⚠️ Production deployment failed — rolling back..."
          gh release delete "prod-${{ needs.build.outputs.version }}" --yes || echo "No rollback needed"

      - name: Notify PR / Issue
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_URL=$(gh pr list --state open --json url --jq '.[0].url' || echo "")
          [ -n "$PR_URL" ] && gh pr comment "$PR_URL" --body "🚀 Production deployment done for $TAG"
